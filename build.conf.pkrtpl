System hostname = openbsd-kernel-aws
Password for root account = *************
Public ssh key for root account = ${ssh_public_key}
Change the default console to com0 = yes
Which speed should com0 = 115200
Start sshd(8) by default = yes
Allow root ssh login = prohibit-password
Do you expect to run the X Window System = no
What timezone are you in = Etc/UTC
Location of sets = cd0
Pathname to the sets = ${iso_sets_path}
Set name(s) = -game* -x*
Set name(s) = done
Directory does not contain SHA256.sig. Continue without verification = yes
Use (W)hole disk MBR, whole disk (G)PT or (E)dit = ${disk_answer}
Location of sets = done
